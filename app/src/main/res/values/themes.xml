<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RoomMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_blue1</item>
    </style>

    <style name="Theme.RoomMate" parent="Base.Theme.RoomMate" />

    <style name="Theme.RoomMate.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/logo_image</item>
    </style>
</resources>